diff --git a/modules/loop-pipeline/amplifier_module_loop_pipeline/validation.py b/modules/loop-pipeline/amplifier_module_loop_pipeline/validation.py
index 6c29cef..65fffc7 100644
--- a/modules/loop-pipeline/amplifier_module_loop_pipeline/validation.py
+++ b/modules/loop-pipeline/amplifier_module_loop_pipeline/validation.py
@@ -1453,7 +1453,21 @@ def _unmarked_passthrough_path_to_exit(
     while queue:
         node_id, has_unmarked, path = queue.popleft()
         if _node_regates(graph, node_id):
-            # Re-gating intermediary: corrective routing — stop this branch.
+            # Re-gating intermediary: corrective routing -- stop this branch.
+            # EXCEPTION: a node that re-gates purely via conditional outgoing
+            # edges (not a human gate) AND simultaneously has a plain
+            # unconditional edge directly to an exit is not a true re-gate --
+            # the plain edge is a silent escape route that fires when the
+            # condition does not match.  Detect that escape and emit a path so
+            # _check_fail_routed_to_exit can flag it.
+            # Human gates (hexagon / wait.human) are exempt: external human
+            # judgment is always a real gate regardless of edge structure.
+            node_obj = graph.nodes.get(node_id)
+            if node_obj is not None and not _is_human_gate(node_obj) and has_unmarked:
+                for edge in graph.outgoing_edges(node_id):
+                    if not (edge.condition and edge.condition.strip()):
+                        if edge.to_node in exit_ids:
+                            return path + [edge.to_node]
             continue
         for edge in graph.outgoing_edges(node_id):
             if edge.condition and edge.condition.strip():
diff --git a/modules/loop-pipeline/tests/test_topological_lint.py b/modules/loop-pipeline/tests/test_topological_lint.py
index 74d7bd3..9f9da95 100644
--- a/modules/loop-pipeline/tests/test_topological_lint.py
+++ b/modules/loop-pipeline/tests/test_topological_lint.py
@@ -1169,8 +1169,8 @@ class TestFailRoutedToExit:
         assert _diag(lint(g), "fail_routed_to_exit")
 
     def test_indirect_regating_intermediary_not_flagged(self):
-        """An intermediary with a condition-bearing outgoing edge re-gates the
-        flow (retry-vs-escalate routing) — corrective, not flagged."""
+        """An intermediary with ALL outgoing edges conditional (no plain escape
+        to exit) re-gates the flow -- corrective, not flagged."""
         g = _graph(
             nodes={
                 "start": _mdiamond(),
@@ -1188,7 +1188,9 @@ class TestFailRoutedToExit:
                 Edge("triage", "done"),
             ],
         )
-        assert not _diag(lint(g), "fail_routed_to_exit")
+        # triage has a plain edge to done (exit): the hazard shape.
+        # A re-gating node with a plain escape route to exit must be flagged.
+        assert _diag(lint(g), "fail_routed_to_exit")
 
     def test_indirect_multihop_one_unmarked_flagged(self):
         """Multi-hop path with one unmarked intermediary among marked ones
