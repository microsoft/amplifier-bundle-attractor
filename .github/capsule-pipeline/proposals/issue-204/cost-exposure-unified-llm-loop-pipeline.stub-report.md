# Stub report -- the reviewer 90-second evidence read (ships beside the capsule; the raw stub diff NEVER does)

stub status: greened
touched top-level surfaces: modules/
numstat (added/removed): 125/2
stub_flipped (feature-AC census rows the stub turned UNMET->MET): [AC-1,AC-2,AC-3,AC-4,AC-5]

The raw stub diff is a working recipe to green this gate WITHOUT building the feature, and the packaged capsule is handed to an autonomous implement stage -- so the diff stays in the run workspace (.ai/hypothesis_v.patch), for the PR reviewer channel only, and never ships beside the capsule. The judge reviewer-plausible/sabotage classification lives in the critique brief packaged beside this report.
