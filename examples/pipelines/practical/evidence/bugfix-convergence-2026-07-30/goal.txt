Fix the planted get_display_name bug in user_service.py with a minimal production-code repair and a regression test for avatar=None. Preserve existing tests; do not weaken or skip them.

This is an intentional convergence demonstration. On the FIRST implement_fix attempt, make the correct fix and regression test so pytest is green, but deliberately leave a newly-created reproduce_bug.py final comment saying '# never reached' after its print even though the fix makes it reachable. The critique must return VERDICT: ITERATE and identify correcting that comment as the highest-leverage next step. The feedback node must write that critique to .ai/feedback/. On the NEXT implement_fix attempt, read the feedback, correct that stale comment, leave the fix and test intact, then let critique judge normally and ship if sound. Do not claim completion or SHIP before this two-pass demonstration has occurred.
